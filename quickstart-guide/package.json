{
  "name": "quickstart-guide",
  "version": "1.0.0",
  "description": "First OpenAI Api code",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist/ && tsc",
    "start": "node dist/index.js",
    "prestart": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.9.4",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "openai": "^4.20.0"
  }
}
